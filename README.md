# Zabbix Docker Lab

Zabbix 6.4 LTS deployed with Docker Compose (PostgreSQL + Server + Web + Agent).

## Stack
- Docker / Docker Compose
- Zabbix 6.4 LTS
- PostgreSQL
- Linux (ARM64)

## Run
docker compose up -d

## Stop
docker compose down
